const types = {
  app: 1,
  page: 2,
  pageComponent: 3,
  component: 4,
}

const DOMAIN_METHODS = "methods"
const EVENT_HOT_READY = "hotReady"

module.exports = {
  DOMAIN_METHODS,
  EVENT_HOT_READY,
  types,
}
